package types.paths;
import elements.SvgElement;
import types.Path;
import visitors.*;
/** 
* @module Arc.java
* @version 1.0 
* @since 2013-04-24
* @author Jimmy Lin (u5223173) - u5223173@uds.anu.edu.au
* 
* Edited by MacVim
* Class Info auto-generated by Snippet 
*/
public class Arc extends Path {
    static final int SIZE_AaCOMMAND = 7;
    
    private Point2D endPoint;
    private double radiusX;
    private double radiusY;
    private double rotationX;
    private double largeArcFlag;
    private double sweepFlag;

    public Point2D getEndPoint() { return this.endPoint; }
    public double getRadiusX() { return this.radiusX; }
    public double getRadiusY() { return this.radiusY; }
    public double getRotationX() { return this.rotationX; }
    public double getLargeArcFlag() { return this.largeArcFlag; }
    public double getSweepFlag() { return this.sweepFlag; }

    public Arc (SvgElement e, String command, double[] points) {
        super(e);
        this.command = command;
        if (this.command.matches("[Aa]")) {
            if (points.length >= SIZE_AaCOMMAND) {
                this.radiusX = points[0];
                this.radiusY = points[1];
                this.rotationX = points[2];
                this.largeArcFlag = points[3];
                this.sweepFlag = points[4];
                this.endPoint = new Point2D (points[5], points[6]);
            }
        }
    }
    // visitor pattern implementation
    public void acceptPV (PathVisitor visitor) {
        visitor.visitArc(this);
    }

    public String toString () {
        String str = "" ;
        str += " " ;
        return str;
    }
}
